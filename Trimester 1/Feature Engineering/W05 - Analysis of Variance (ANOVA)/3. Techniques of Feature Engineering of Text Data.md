Now let's look at some more techniques that are commonly used in text feature  

engineering. We will cover unigrams, n-grams, one hard encoding, and pause  

tagging. All of these techniques help transform raw text into features that  

machine learning models can learn from. They are core steps in feature  

engineering for NLP, just like scaling or encoding is for tabular data. So  

unigrams and n-grams are feature extraction techniques that convert text  

into numerical features. For example, We can represent text using unigram,  

single word counts, or bigram, which is a pair of word counts. N-grams capture  

local contexts and word combinations. For example, New York City as a trigram  

gives more insight than individual words. Bigrams and trigrams help detect  

phrases like machine learning, data science, etc. They help the model learn  

patterns like great movie or not good.  

One hot encoding converts categorical data into binary vectors. For text, one  

hot encoding can mean turning each word into a unique vector with a single one.  

Part of speech tagging or pause tagging labels each word with its grammatical  

role, which is noun, verb, adjective, etc. These tags can be used as features  

in models. For example, we might count the number of adjectives in a text to  

predict sentiment. Advanced NLP pipelines use pa stags to do parsing or  

sequence models. Let's look at this code for.  

Demonstrating unigrams, n-grams, and post tagging, we're using the  

countervectorizer from SK Learn convert text in feature metrics, right? Again,  

we are using the clean text from our small data set, the subset that is 2000  

records from our 20 news groups data set on which we had done some cleaning  

earlier. This cleaning that we had done and we created underscorem, and this  

column clean text will. Have those clean text in data records for the  

20001st, 2000 records in the 20,000 news groups data set. So that is what  

we're using here also for unigrams, for demonstrating unigrams and n-grams.  

This unveizer by default creates unigram features, and if you do afield  

transform of that on the clean text, then it will give us the unigrams and  

its stores in this x_uni variable. This shape of the unigram feature metrics is  

2000.  

25,307. That means 2000 records and 25,307 unigram features have been  

generated.  

We're printing the 1st 10 unigram features, and that's like AA. A A A AI  

and so on. There's really no meaning that we can interpret from this, but  

this is a way in which the documents have been converted into unigram  

features. The next thing is n grammes. We're gonna see is n grammes, which is  

unigrams plus bigrams plus trigrams, and we're using the same count  

vectorizer here, but a different range. We're specifically saying extract the  

up n grammes or +13 words, using the count vectorizer. And when we do a feed  

and transform on the pretext, what we will get is the As and grammes with the  

particular geometric shape with 2000 records and 30 features. And then we  

are displaying the top 30 grammes feature, which is this also could even,  

if you look at this output, you see that some of them are unigrams, like  

also could even these are unigrams, whereas this one max max, that's a  

bigram, and then this one max max max is a trigram, right? So it's a  

combination of unigrams, bigrams, and trigrams that we're getting as output  

because we specified that we want all of them.  

Into the convectorizer API, the next thing we will see is that we talked  

about is one hot encoding that's used for converting categorical labels into  

binary vectors. We're not using the 2 news groups data set, but for example,  

we have a list of three words, which is science, religion, and hockey, and we  

want to convert into one hot encoding mechanism. We want to apply a one hot  

encoding mechanism on this. We use this API, one hot encoder from SK Learn. And  

then we do a feed and transform of this word array. What the output that we  

will get is the one hot encoded output, which is like this. There are 3 words  

that we had one word, so we have of 3 dimensions, right? And one of them will  

be set 1. So the last one being 1 stands for science, the middle one  

being 1 stands for religion, and the first. And being 1 stands for hockey.  

The remaining values are all zeros, right? If you had one more word, for  

example, if we had movies, then the vector would become 4 values right now  

instead of 3, there'd be 4 values, and each word will have any one position,  

and the remaining positions will have a 0. That's the.  

you know, that's the idea behind one heart and Cody.  

Each word becomes a vector with 1 at its own index and 0 everywhere else,  

and that helps convert nominal categories into machine learning form.  

Last thing that we're gonna see is pause tagging, a part of speech tagging,  

uh, which hits in grammatical analysis, that is to extract nouns, verbs,  

adjectives, prepositions, etc. We are doing this only on one particular  

document. The first document, we are using the first document of the upset  

that we created. And we're doing a recognising on that, and then we are  

applying pause tag, right, pause underscore tag. So the first step is  

this is for simplicity, using the first document is only fitting simplicity,  

but the first step to do is tokenize the document, one document, and then  

apply pa tagging on the tokens, right? The tokens is what is going as input  

into the patag API. All of these are coming from the NLTK libraries, right  

here we are importing the NLTK libraries cross tag inver organise is  

coming from NLTK and what does the uh sample atkins look like? This is our  

kins  

for well is RB, right? And SDP is AJ site is NN. So all of these tags, NN  

stands for noun, BJ for a adjective. RB for adverb, BBD is for verb, past tense.  

Everything, every ad here has a meaning, and so this particular API is used,  

pause tagging is used. It tells us what is the grammatical meaning of every  

word in a document. So if you plots the frequency in the sample document, we  

know that in that one particular document that we picked up, there are  

about 16 nouns, and then there are about 8 verbs in the sentence. About  

verbs in the past tense and so on. So far what we've seen is techniques like  

unigrams, n-grams, um, one hard encoding and past tagging as ways to  

convert text into numeric features.
