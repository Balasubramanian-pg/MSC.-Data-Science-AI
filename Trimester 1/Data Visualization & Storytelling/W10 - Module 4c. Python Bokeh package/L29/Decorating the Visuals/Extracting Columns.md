# Extracting Columns

```python
freight = monthly_values_df["freight"]
mail = monthly_values_df["mail"]
```

Now:

|Variable|Meaning|
|---|---|
|`x`|month indices|
|`freight`|freight values|
|`mail`|mail values|
