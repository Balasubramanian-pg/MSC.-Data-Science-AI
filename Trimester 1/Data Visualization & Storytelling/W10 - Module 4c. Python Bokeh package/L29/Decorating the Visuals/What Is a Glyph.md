# What Is a Glyph?

A glyph is:

> a visual object rendered on the plot

Examples:

|Glyph Function|Visual|
|---|---|
|`circle()`|scatter points|
|`line()`|line chart|
|`vbar()`|bars|
|`rect()`|rectangles|
