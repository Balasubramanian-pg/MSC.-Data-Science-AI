# Hexadecimal Colors

Hex codes provide exact control.

```python
plt.plot(x, y, color='#FF5733')
```

Structure:

```text
#RRGGBB
```

Where:

- RR = red intensity
    
- GG = green intensity
    
- BB = blue intensity
    

Each ranges:

$$
00 \to FF
$$

Equivalent to:

$$
0 \to 255
$$
